"""Data-model package: the typed object graph that ties anatomy and geometry
into a single auditable unit of work (`Case`).

Top compositional layer. Imports from `anatomy/` and `kernel/`; nothing in
`anatomy/` or `kernel/` may import from here (CLAUDE.md package boundaries).
"""
